[pytest]
# importlib mode is required: tests/{base,bebopnet,cmt,mingus,integration}/__init__.py
# turn each test dir into a top-level package, and `bebopnet`/`cmt`/`mingus`
# collide with models/{bebopnet,cmt,mingus} in the default "prepend" mode.
addopts = --import-mode=importlib
